extends layout

block content
  .main
    h2 👥 Users

    if users.length === 0
      p No users found.
    else
      each user in users
        .user-card
          a(href=`/users/${user.id}`)
            h3= user.username
          p 📄 Posts: #{user.post_count}